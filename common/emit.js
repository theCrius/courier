/**
 * Broadcast a message to a room
 * @param {req} req , The client request.
 * @param {string} label , The event name to identify the message
 */
function emit(req, label) {
  //TODO: whole lotta stuff here
}

module.exports = emit;
